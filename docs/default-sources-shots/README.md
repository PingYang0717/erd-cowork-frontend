# 預設資料來源(2026-09-18)

對應 feat/iframe-postmessage 上的 commit「預設資料來源進偏好設定;來源種類綁定後不變(ADR-0018)」。

- `menu-light.png` / `menu-dark.png`:頭像偏好選單多一列「預設資料來源」,值是選了幾個。
- `panel-light.png` / `panel-dark.png`:點進去開的編輯器,跟對話的 Connectors 面板同一個;副標寫明只是新對話的預選、不會自動掛到任何對話上;按鈕是「儲存」不是「送出」。
