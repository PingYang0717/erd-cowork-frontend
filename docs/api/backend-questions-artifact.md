# 給後端的問題清單:Artifact 的持有、釘選與排程

日期:2026-08-29,**2026-08-31 大幅更新**。每一題都是**前端無法自行決定、且會卡住
設計**的點——不是功能許願,是契約上的模糊或缺口。依阻塞程度排序。

> **2026-08-31 更新摘要**。Gallery 的設計已定案為**平坦化**(Artifact 是獨立個體,
> 不以 session 分組,仿 Claude),七項決議見 `docs/artifact-model-decisions.md`。
> 這改變了本清單:
>
> **題號已重排**,以下用「原 → 現」對照:
>
> - **原 6(lineage)→ 現 2,並升格為最高** — 從「低優先、影響長期」變成新設計的
>   **必要條件**:平坦化之後,「版本」唯一的意思就是同一份分析跑不同時間段,而那需要
>   lineage 才表達得出來。
> - **原 2(排程歸屬)→ 現 3,並改寫** — 平坦化之後問題變了:不再是「產出掛在哪個
>   session」,而是「排程能不能綁定一個 artifact 重複執行」。
> - **原 4(`GET /directory`)→ 現 5,端點部分已解決** — 確定為
>   `GET /hr/employeesAndOrgs?key={}`,前端已改為滿 3 字元才搜尋。**但權限那一半仍未
>   回答**,保留著。
> - **原 3 → 現 4、原 5 → 現 6**,內容不變。
> - **新增現 8〜10** — unpublish 的撤銷語意、資料源端點形狀、重跑端點。

---

## 1【最高】`Artifact.pinnedAt` 是全域欄位還是 per-user?

**契約現況矛盾。** `Artifact` 上 `pinnedAt` 是單一欄位:

```ts
/** 釘選的時間戳(ISO 8601),未釘選為 null。 */
pinnedAt: string | null;
```

> **2026-09-01 更新**:前端已移除 `canPin`。釘選是讀者自己的書籤,不是擁有者授予的權限,
> 所以按鈕永遠可按——而那個欄位一旦在某次回應裡缺席,就會把按鈕誤停用。它原本的措辭
> (「the signed-in user」)也正是下面這個矛盾的線索。

**為什麼卡住我們**:分享來的 Artifact(`isOwn === false`)如果被我釘選——

- 若 `pinnedAt` 是**全域**的 → 我改到了那份 artifact 的欄位,**擁有者會看到他的
  Artifact 被我釘了**。這是現在就存在的問題,與 Gallery 改版無關。
- 若是 **per-user**(同一份 artifact 對不同呼叫者回傳不同 `pinnedAt`)→ 一切通順,
  釘選是個人書籤。

**我們的立場:釘選應該是個人化行為。** 「我要常看這個」是讀者的事,不是物件的屬性;
一份被十個人分享的報表,十個人各自的釘選不該互相覆蓋。

**請確認**:`pinnedAt` 目前的實作是哪一種?若是全域,能否改為 per-user?

**前端的因應**:若確認要改 per-user,我們照現況實作即可(API 形狀不變,只是語意
釐清)。若短期不改,我們會在 `isOwn === false` 時**隱藏釘選入口**當臨時緩解——
但那會讓「把別人分享的報表加進我的常看清單」這件事做不到。

同一個問題也適用於 **`Session.pinnedAt`**(Studio 左欄的 Pinned 分組用它)。session
不會被分享,所以目前沒有暴露問題,但語意最好與 artifact 一致。

---

## 2【最高】同一份分析的版本之間沒有任何關聯欄位(lineage)

**升格自原第 6 題。** 平坦化之後,「版本」有了明確且唯一的意思:**同一份分析跑不同時間
段的資料**(聊天迭代則產生各自獨立的 Artifact,不是版本)。

而系統裡沒有任何欄位能表達這件事。最小的後端改動是給 `Artifact` 加一個 lineage 欄位
(例如 `rootArtifactId`,重跑時繼承)。

**為什麼是必要條件**:沒有它,同一份分析的三次時間段重跑就是三張看起來一樣的 Gallery
卡,而不是一張卡點進去可切換。

**一個設計約束**:版本可能由**手動重跑**產生,也可能由**排程**產生(見第 3 題),兩者
都要能支援。所以版本記錄最好能標示產生方式,否則之後補排程會撞到。

---

## 3【高】排程能不能綁定一個 Artifact 重複執行?

`ScheduleJob` 的型別已在前端定義(`id` / `title` / `cadence` / `lastRunAt` / `status` /
`scenario`),`/cowork/schedule` 是佔位頁,後端無任何端點。

**平坦化之後問題改變了**:不再是「排程產出掛在哪個 session」,而是——排程能不能綁定
**一個既有的 Artifact**,每次執行用它的邏輯跑新的時間區間,產出成為那個 Artifact 的新
版本?

**需要的欄位**:`ScheduleJob.artifactId`。

**為什麼要問**:這是每日晨會情境的正解——排程前一晚跑好,人到了直接看最新一版。若排程
每次都產生一個全新的、無關聯的 Artifact,Gallery 三十天後仍會被淹沒。

**這一期不做**,但架構不能擋住它,所以現在要知道方向。

---

## 4【中】`Artifact.type`(dashboard / slides)何時回到契約?

定版時暫時拿掉,至今未加回。**前端現況**:Gallery 卡片完全不顯示縮圖與 Dash/Deck
標籤——與其讓每張卡都預設成同一個猜測,不如都不顯示。

**請確認**:是否仍計畫加回?若是,欄位名與值域(`'dashboard' | 'slides'`)可否照
原定?

---

## 5【中】收件者名單的**權限層級**(端點本身已解決)

**端點已確定**:`GET /hr/employeesAndOrgs?key={}`,取代原先規劃的 `GET /directory`。
前端已改為搜尋式:滿 3 個字元才發請求(目錄是全公司,一兩個字元會命中大半),250ms
debounce,比對交給後端。原本那份假名單已從 `src/api` 移到 `src/mocks`。

**仍未回答的是權限**:

- 回傳的是「組織全體」還是「**我有權分享給的人**」?
- 後端會不會驗證 `POST /artifacts/:id/share` 的 `targetIds` 合法性,還是信任前端送
  什麼就是什麼?

對 fab 資料而言這不是小事:若名單是組織全體且後端不驗證,任何人都能把報表分享給任何
人。

---

## 6【中】分享的層級——每次產出都要重新分享一次嗎?

> **2026-08-31 註**:Q1 定案「聊天迭代產生獨立 Artifact」之後,這題**更尖銳了**——
> 迭代五次就是五個 Artifact,每個都要分別發布與分享。若第 2 題的 lineage 成立,「綁
> lineage」會是最自然的答案。

**現況**:分享綁在單一 Artifact 上。而每次迭代/排程執行都產生**新的 Artifact**
(版本即獨立 Artifact),所以昨天分享過的對象,對今天新產出的那一份**沒有存取權**。

以每日晨會為情境:三十天 = 三十次手動選收件者。

**請討論**:分享的對象應該綁在什麼層級?

- 綁 Artifact(現況)→ 每次重做
- 綁「同一份分析的所有版本」→ 需要 lineage 概念(見第 2 題)
- 綁 Session → 整串對話的產出對同一群人開放
- 綁 ScheduleJob → 排程設定一次,之後每次產出自動沿用(與第 2 題相關)

---

## ~~7~~【已升格為第 2 題】同一份分析的版本之間沒有任何關聯欄位

**現況**:版本不是後端資源。前端靠「同一個 session 裡帶 `artifactId` 的訊息順序」
推導版號(`deriveArtifactVersions`)。這帶來兩個已知限制:

- **版號會漂移**:刪掉中間某一版,後面全部往前挪(位置即版號)。
- **只有在 session 上下文裡推導得出來**:Gallery 沒有那個上下文,所以同一份分析的
  三個版本在 Gallery 是三張看起來幾乎一樣的卡。

**若未來要改善**,最小的後端改動是給 `Artifact` 加一個 lineage 欄位(例如
`rootArtifactId`,迭代時繼承)。這樣版號可以穩定、Gallery 可以摺疊、分享也能綁在
lineage 層(第 5 題)。

**這題不急**,但如果第 2、5 題要動資料模型,值得一起考慮。

---

## 8【待確認】分享端點的兩處拼寫

**路徑單複數。** 前端一律用 **`/artifacts/{id}/share`**(複數),與
`/artifacts/{id}/publish`、`/artifacts/{id}/pin` 一致。規格討論時寫的是
`/api/artifact/{id}/share`(單數),推測是筆誤,但若後端真的是單數請告知——這是一改就會
全錯的那種差異。

~~**`shareTargetType` 還是 `sharesTargetType`?**~~ **已確認:一律單數**
(`shareTargetType`),前端已收斂。

---

## 9【高】刪除必須撤銷已發出的存取權

**前端已決定**(決議 Q3、Q4 修訂版):發布是分享的前提,而**取消發布這個動作不存在**
——把 Artifact 從架上拿下來和刪掉是同一件事,所以只有 Delete。`DELETE
/artifacts/{id}/publish` 前端已不再呼叫。

**請確認**:`DELETE /artifacts/{id}` 是否(或能否)連帶讓已分享出去的連結立即失效?
這是「分錯人了趕快收回」唯一的手段。

---

## 10【中】`data-source` 端點的形狀確認

**前端已實作,mock 先行**(決議 Q5):資料源附掛在 **session** 上,不是使用者的全域
偏好——兩段對話可以用不同的資料源。

- `PATCH /sessions/{id}/data-source` 附掛一個
- `DELETE /sessions/{id}/data-source` 卸除一個
- `SessionDetail` 多一個 `dataSourceIds: string[]`

**請確認**:

- 這個形狀是否符合後端規劃?
- `DELETE` 的 `connectorId` 目前走 **request body**(因為兩個方向共用同一路徑)。要
  改成 query(`?connectorId=`)或 path(`/data-source/{connectorId}`)嗎?
- `SessionDetail.dataSourceIds` 目前在前端標為 **optional**,少了它讀作「沒有附掛任何
  資料源」——因為這個欄位還沒上線,少了它不該讓整個 thread 崩掉。上線後會改回必填。

---

## 11【中】重跑端點:用同一份 Artifact 跑另一個時間區間

決議 Q6 的另一個必要條件。需要一個「用 artifact X 的分析邏輯 + 這個時間範圍,產生新
版本」的端點(形狀未定,例如 `POST /artifacts/{id}/runs` 帶時間範圍)。

**背景**:artifact 的資料在後端組裝時就烤進 HTML(CSP 讓 iframe 內的 script 無法重新
查詢),所以換時間段一定是**後端重新組裝一份新的 HTML**,前端做不到。

**這一期不做**,但與第 2 題的 lineage 是同一組設計,值得一起考慮。

---

## 附:設計脈絡(為什麼這些問題現在浮出來)

> **2026-08-31 更新**:以下這段記錄的是**已作廢**的方向(以 session 分組)。最終定案
> 是**平坦化**——Artifact 是獨立個體,使用者選擇哪些要發布,發布了才出現在 Gallery,
> 一個 Artifact 一張卡。保留這段是為了讓上面幾題的來歷讀得懂。

前端曾評估把 Artifact Gallery 從「每個 Artifact 一張卡」改成「每個 Session 一張卡,
點進去用既有的版本選單切換」。動機是每日晨會這類重複性情境——目前每天的產出都是一張
新卡,三十天後 Gallery 不可用。

**改採平坦化之後,那個動機由「發布」承擔**:只有刻意發布的才上架,所以 Gallery 不會被
每天的隨手產出淹沒;而重複性情境的正解是排程綁定 artifact(第 3 題)加上 lineage
(第 2 題),讓晨會報表始終是同一張卡的最新一版。

已經確定的邊界(不需後端配合):

- **`Shared to me` 維持扁平**——別人的 session 存取一律 404,分享來的 Artifact 本來
  就是一份獨立成品,沒有歷史可看。全頁檢視的程式碼已經這樣分岔(`isOwn === false`
  時不渲染版本選單,改顯示分享者與「Shared to me」標記)。
- **Artifact 是資料烤死的成品,不是版型**。資料在後端組裝時就注入 HTML,iframe 內的
  script 無法重新查詢。所以分享 = 分享那批資料的**快照**,權限判斷只在「能不能拿到
  這份 HTML」那一層。這點我們已理解並接受,列在這裡是為了確認雙方認知一致。

  **更正(2026-08-29 安全審查)**:先前這裡寫「CSP `connect-src 'none'` 讓 script
  完全無法發出請求」是過度宣稱。`connect-src` 擋 fetch/XHR/WebSocket,但**擋不住
  iframe 導覽自己**(`location.href = 'https://…?' + data`)——sandbox 缺
  `allow-top-navigation` 只擋頂層導覽,CSP 也沒有能擋這件事的指令。也就是說,一份
  惡意或被污染的 artifact **仍有外送資料的路徑**。這不改變「分享=快照」的結論,但
  改變了它的安全邊界:我們無法在前端保證 artifact 不外洩它收到的資料。**若這對
  fab 資料是不可接受的,需要後端在組裝時消毒 agent 產出的 HTML** — 這一項待雙方
  決策,見安全審查紀錄。
