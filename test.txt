關於資料夾結構也要調整 src 底下會有 api (axios client + endpoint modules, 底下拆分 apiclient/ agentApi/ sessionApi ...), components (UI components by domain, 底下拆分 artifact/chat/common/layouts ....),config,constants/hooks/theme/types/utlis/

＃componet structure
-all components use React.FC<Props> with a TypeScript props interface
-components file order: props interface->hook->handlers(usecallback)->render->'export default'
-use react.lazy(()=>import('...'))+<suspenseloader> only for independent routes or heavy third-party components
-

#data fetching
-primart data fetching must use useSuspenseQuery - not useQuery+isLoading
-api client centrlized in src/api , use shared aplClient
-Mutation errors hanled via onError callback, useSuspenseQuery errors caugh by <ErrorBoundary>

#performace
-event handles passed to child components must be wrapped in usecallback
-expensive computations:'useMemo'. expensive components:'React.memo'
-Search inpust: debounce 300-500ms
-'useeffect' must return a cleanup function to prevent memory leaks

#import aliases
- use '@/' alias (points to 'src/', defined in 'vite.config.ts'+'tsconfig.app.json')
-no other custom aliases

#multi-user indentity
- all api requests carry 'x-user-id' header(v1: anonymous UUID from localStorage,attached by axios interceptor)
-internal enviorment: SSO/gateway injects the header instead

請確保所有功能不受影響並follow以上規則