node_modules
dist
dist-ssr
*.local
.env

# editor
.vscode/*
!.vscode/extensions.json
.idea

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# msw regenerates this on install; nothing registers it (ADR-0006 — no runtime
# mock), and public/ ships verbatim, so it must not come back in by accident.
public/mockServiceWorker.js
